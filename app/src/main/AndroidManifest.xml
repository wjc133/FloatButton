<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.elite.floatbutton" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".test.TestObserverActivity"/>
        <activity android:name=".ui.AppBarActivity"/>
        <activity android:name=".ui.ViewAnimActivity"/>
        <activity android:name=".ui.LoginActivity"/>
        <activity android:name=".ui.TransitionActivity"/>
        <activity android:name=".ui.MusicActivity"/>
        <activity android:name=".ui.MusicGirdActivity"/>
        <activity android:name=".ui.TouchActivity"/>
    </application>

</manifest>
